{
  "name": "cnc-insert-manager-v37",
  "version": "37.2.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js && npm --prefix functions test",
    "check": "node --check v31-core.js && node --check access-control.js && node --check admin-app.js && node --check operator-app.js && npm --prefix functions run lint",
    "audit": "npm --prefix functions audit --package-lock-only --omit=dev"
  }
}
